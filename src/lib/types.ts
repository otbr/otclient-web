/**
 * A value in screen-space pixels. Alias for documentation only — TS won't
 * stop a non-pixel number from being assigned. Use at API boundaries to
 * distinguish pixel coords/sizes from tile coords, zoom factors, etc.
 */
export type Pixel = number;
